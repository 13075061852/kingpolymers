# kingpolymer Twin-Screw Configurator

## 启动

### macOS
双击 `启动软件.command`。浏览器会自动打开：

- 本机：`http://127.0.0.1:8731`
- 局域网其他电脑：启动窗口中会显示类似 `http://192.168.x.x:8731` 的地址

首次双击如果被系统阻止：右键文件 → 打开 → 再确认打开。

### Windows
1. 安装 Python 3.11 或更高版本，安装时勾选“Add Python to PATH”。
2. 双击 `start_windows.bat`。
3. 保持命令窗口开启。

## 标准设计流程

1. 主界面上半区是 `Graphic configuration` 图形窗口，下半区依次为元件库、螺杆配置表和机筒配置表。
2. 点击顶部图纸资料图标选择50机或60机并填写图纸名称、版本、日期和材料。新建图纸的螺杆配置为空，不自动放入任何螺纹元件；需要历史方案时再从“历史模板”手动载入。
3. `Machine configuration` 默认显示螺纹元件。单击元件图标立即加入螺杆，点击“−”或右键图标删除最后一个同型号元件；鼠标也可拖入上方机筒。手机上“i / ＋ / −”按钮常显，点 i 可放大查看完整型号及参数。
4. 中间 `Screw` 工具栏提供历史组合、计算校验、插入、替换、删除、复制、方向切换、资料、清空、打印和Excel功能。机筒内已有元件可直接左右拖动。
5. 中下 `Screw` 表显示安装顺序、长度和累计位置，可拖动行、复制、上下移动或删除。
6. 右下 `Barrel` 严格显示原图数据：50机A～N共14个位置（含1mm Wärmesperre和27mm法兰），60机A～M共13个位置（含30mm法兰）。仅自然排气可切换ATM/CLOSED；侧喂和VAC保持固定。
7. 元件拖出机筒后松开会自动删除；也可选中后按 Backspace 或 Delete。
8. 螺杆头随组合总长度移动，始终连接出料端；颜色区分GFA、GFF、KB和GFM类型。
9. 图形工具栏可按75%～200%缩放或全屏检查。
10. 底部状态栏显示总长、安全校验和当前选中元件。
11. 保存草稿；正式生产时到“历史项目”发布并扣减库存。
12. PDF固定导出三页莱斯式工程图：第1页组合总图；第2页机器参数和可用元件分类；第3页螺杆配置表、机筒配置表及安装图。版式以`2024-4-2-ZSE_60CC_05047144__48D_1E...pdf`为基准。
13. 也可导出PNG、Excel或JSON工程文件。

v1.17：Machine元件库及Elements预览改用完整型号的SVG参数示意，不再放大旧截图或借用相近型号。按各机型直径及元件长度保持比例，KB分别显示实际片数、错位角和方向；KS仅表示单片轮廓与长度，不推测A/E、M后缀的加工外形。图标可无损缩放，但属于工程示意，外形仍待供应商图纸终核，不是制造CAD。组合总图及PDF/PNG导出仍保留此前原图外观，本次不改历史项目、元件ID、库存及安全规则。当前50/60机元件库共70个型号（50机40种、60机30种）。

## 手机与平板

- 手机端按图形、Machine、Screw、Barrel纵向排列；平板在空间允许时将Screw与Barrel并排。
- 工具栏自动换行，按钮增大；宽表格在面板内部左右滑动，表头与行同步，不撑宽整页。
- 点击元件加入，使用Screw列表的↑、↓、复制、删除按钮操作。手机在图形上滑动用于滚动/缩放，不触发“拖出删除”，避免误删。
- 输入`kb-6-2-60-60度re`或`ks-1-2-10-ae`也可搜索对应型号。
- 本机服务刷新页面即可加载v1.17；手机须与主机同一局域网并访问主机IP（不能使用手机自己的127.0.0.1）。公网部署需另行同步前端文件。
- 自动化回归：`node test_component_models.js`；安装Playwright及Chromium/WebKit后运行`node test_responsive.js`与`BROWSER=webkit node test_responsive.js`。覆盖320–1920像素屏宽、横竖屏、两种机型、搜索增删、表格及打印；这不替代真实手机的最终验收。

型号维护统一在软件顶部“元件库”中操作：右上角“＋ 添加型号”用于添加；每一行的“编辑”和“删除”用于修正或停用错误型号。删除只从当前元件库停用，不删除历史项目和原始图纸。

## 安全规则

自然排气打开时、固定侧喂料口和固定真空口的实际开口投影范围及前后各30mm，只允许使用GFA。自然排气使用堵头关闭后取消该位置限制。

违规元件显示红色。强制保存或发布时必须：

- 填写放行原因；
- 输入负责人确认码；
- 导出PDF会标记为非标准组合。

确认码提示：负责人电话号码，尾号0731。

## 数量与库存

- “组合位置/套”：代表图中一根螺杆的排列位置数量。
- “实际单件/件”：双螺杆实际所需数量，等于套数×2。
- 库存按实际单件管理。
- 保存草稿、导出文件不扣库存。
- 正式发布扣库存；作废已发布项目自动退回库存。
- 库存未盘点的元件显示“未录入”，发布时不自动改变该型号库存。

## 旧图纸导入

支持：

- 莱斯特瑞兹三页PDF；
- XLSX排列清单；
- 本软件导出的JSON工程文件。

导入后必须检查识别预览、元件顺序和总长度。扫描版PDF可能需要人工修正。

## PDF导出

点击“导出PDF”后会打开浏览器打印窗口：

- 目标打印机选择“存储为PDF”或“Microsoft Print to PDF”；
- 纸张选择A4；
- 方向选择横向；
- 缩放选择100%；
- 关闭浏览器自带页眉页脚。

生成内容：

1. 完整组合图；
2. 元件型号、套数、实际件数、库存与缺少数量；
3. 安装顺序、累计坐标、机筒配置与组合图。

## 数据与备份

- 数据库：`data/guangjun_screw.db`
- 软件每天首次启动时自动备份，保留最近30份；
- 手工备份：设置 → 立即备份；
- 备份目录：`backups/`。

迁移到Windows时，关闭软件后复制整个“螺杆组合软件”文件夹即可。

## 固定机器参数

| 机器 | 机筒 | 图纸总坐标 | 螺纹元件总长度 |
|---|---:|---:|---:|
| ZSE 50CC Pro | 12×210mm | 2548mm | 2590mm |
| ZSE 60CC | 12×240mm | 2910mm | 2970mm |

50机、60机元件库严格分开，不能跨机器调用。
